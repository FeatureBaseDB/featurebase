version: '3.9'

services:
  featurebase:
    image: docker.io/featurebasedb/featurebase:3.33.0
    ports:
     - "10101:10101"
    volumes:
      - "featurebase:/data"

volumes:
  featurebase:
    driver: local

networks:
  default:
    name: fbnet
    external: true
